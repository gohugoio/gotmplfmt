# Test reading from stdin when no args are given.

stdin input.html
gotmplfmt
cmp stdout expected.html

# Test that -w with stdin fails.
! gotmplfmt -w
stderr 'error: cannot use -w with standard input'

-- input.html --
<div>
  <span>hello</span>
</div>
-- expected.html --
<div>
	<span>hello</span>
</div>
